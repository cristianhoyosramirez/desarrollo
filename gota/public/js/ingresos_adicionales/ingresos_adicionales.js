function ingresos_adicionales() {
   /*  let cliente = document.getElementById("ingresos_adicionales");
    cliente.style.display = "block";

    let home = document.getElementById("home");
    home.style.display = "none";

    let url = document.getElementById("url").value;

    let usuario = document.getElementById("id_de_usuario").value;
    $.ajax({
      data: {
        usuario
      },
      url: url + "/" + "clientes/index",
      type: "POST",
      success: function(resultado) {
        var resultado = JSON.parse(resultado);
        if (resultado.resultado == 1) {

          $('#list_clientes').html(resultado.clientes)
        }
      },
    }); */


$("#modal_ingresos").modal("show");






  }